# Multi-LLM MCP Client Requirements
# Install with: pip install -r requirements-clean.txt

# Core dependencies
langchain-core>=0.2.0
langgraph>=0.1.0
httpx>=0.25.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# OpenTelemetry (optional, for tracing)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

# LLM Providers (install only the ones you plan to use)

# OpenAI (ChatGPT)
langchain-openai>=0.1.0

# Anthropic (Claude) - uncomment to use
langchain-anthropic>=0.1.0

# Google (Gemini) - uncomment to use  
langchain-google-genai>=1.0.0

# Ollama (Llama) - uncomment to use
langchain-ollama>=0.1.0

# DeepSeek uses OpenAI-compatible API (already included with langchain-openai)

# HTTP client
requests>=2.31.0
